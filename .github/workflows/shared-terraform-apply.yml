name: Terraform Apply

on:
  workflow_call:
    inputs:
      gcp_project_id:
        required: true
        type: string
      gcp_region:
        required: true
        type: string
      wif_provider:
        required: true
        type: string
      wif_sa:
        required: true
        type: string
    outputs:
      apply_result:
        description: "Results from terraform apply in JSON format"
        value: ${{ jobs.apply.outputs.apply_json }}


jobs:
  apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    outputs:
      apply_json: ${{ steps.parse_output.outputs.apply_json }}

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Authenticate with Google Cloud
        id: gcp-auth
        uses: google-github-actions/auth@v2
        with:
          project_id: ${{ inputs.gcp_project_id }}
          workload_identity_provider: ${{ inputs.wif_provider }}
          service_account: ${{ inputs.wif_sa }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.11.4"

      - name: Download Terraform Plan
        uses: actions/download-artifact@v4
        with:
          name: tf-plan
          path: .infrastructure/tf.plan

      - name: Create terraform.tfvars file
        id: create-tfvars
        working-directory: .infrastructure
        run: |
          cat <<EOF > terraform.tfvars
          repository_name      = "${{ github.repository }}"
          project_id           = "${{ inputs.gcp_project_id }}"
          region               = "${{ inputs.gcp_region }}"
          EOF
          terraform fmt terraform.tfvars

      - name: Terraform Init
        id: terraform-init
        working-directory: .infrastructure
        run: terraform init

      - name: Terraform Apply
        working-directory: .infrastructure
        run: |
          terraform apply tf.plan
          terraform output -json > apply_output.json

      - name: Parse Terraform Output
        id: parse_output
        working-directory: .infrastructure
        run: |
          if [ -f apply_output.json ]; then
            # Escape newlines and quotes for GitHub Actions
            APPLY_JSON=$(cat apply_output.json | jq -c .)
            echo "apply_json=${APPLY_JSON}" >> $GITHUB_OUTPUT
          else
            echo "apply_json={}" >> $GITHUB_OUTPUT
          fi